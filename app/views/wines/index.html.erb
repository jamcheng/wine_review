<h1>Wines</h1>
<p>Available as of <%= formatted_time(@available_at) %></p>
<ol>
	<% @wines.each do |wine|%>
		<li>A <%=wine.varietal%> named <%= wine.name %> from <%= wine.winery %></li>
	<% end %>
</ol>